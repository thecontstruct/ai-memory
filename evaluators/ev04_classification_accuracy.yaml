# Evaluator: EV-04 Classification Accuracy
# Assesses whether the LLM classifier assigned the correct type to content.
# 10% sampling — classification is medium volume.
#
# PATH B (Langfuse V3): Filter by observation name (event_type).
# Tags are trace-level only in V3; use event_types for observation filtering.
# CRITICAL: score_type is CATEGORICAL (NOT BOOLEAN).

id: EV-04
name: classification_accuracy
score_type: CATEGORICAL
target: observation
sampling_rate: 0.10

categories:
  - correct
  - partially_correct
  - incorrect

filter:
  # 9_classify is the event_type for LLM classification observations:
  #   classification_worker.py, process_classification_queue.py
  event_types:
    - "9_classify"

prompt_file: ev04_classification_accuracy_prompt.md

description: >
  Evaluates whether the LLM classifier correctly assigned the memory type
  and collection for a piece of captured content.
  Score is CATEGORICAL: correct | partially_correct | incorrect.
