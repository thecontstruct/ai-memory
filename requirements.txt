# AI Memory Module - Core Dependencies
# Python 3.10+

# Vector Database
qdrant-client==1.17.1

# HTTP Client
httpx==0.28.1

# Environment Configuration
pydantic==2.12.5
pydantic-settings==2.13.1

# Logging
structlog>=24.0.0,<26.0.0

# Monitoring (optional but included in core)
prometheus-client==0.24.1

# Anthropic SDK for conversation capture (TECH-DEBT-035)
anthropic==0.89.0

# Retry logic with exponential backoff (DEC-029, TECH-DEBT-041)
tenacity==9.1.4

# Password hashing for Prometheus auth (install.sh generate_prometheus_auth)
bcrypt>=4.0.0,<6.0.0

# AST-based code chunking (TECH-DEBT-052)
tree-sitter>=0.21.0,<1.0.0
tree-sitter-python>=0.21.0,<1.0.0
tree-sitter-javascript>=0.21.0,<1.0.0
tree-sitter-typescript>=0.21.0,<1.0.0
tree-sitter-go>=0.21.0,<1.0.0
tree-sitter-rust>=0.21.0,<1.0.0

# Token counting for smart chunking
tiktoken>=0.5.0,<1.0.0

# Numpy for vector operations (topic drift computation)
numpy>=1.26.0,<3.0.0

# Security scanning - Layer 2 secret detection (SPEC-009)
# Required for hook scripts (run from project venv)
detect-secrets>=1.4.0

# Security scanning - Layer 3 NER-based PII detection (SPEC-009)
# en_core_web_sm model downloaded in install.sh post-install step
spacy>=3.8.0,<4.0.0

# Cron schedule parsing for evaluator-scheduler daemon (S-16.5, DEC-110)
croniter>=2.0.0,<7.0.0

# LLM Observability — Langfuse tracing (SPEC-020, PLAN-008)
langfuse>=4.0.0,<4.1.0

# Auto-instrumentation for Anthropic SDK calls (SPEC-020 §6)
opentelemetry-instrumentation-anthropic>=0.1,<1.0.0
