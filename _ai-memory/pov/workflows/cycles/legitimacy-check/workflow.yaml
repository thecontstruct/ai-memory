# Legitimacy Check Cycle Workflow
name: legitimacy-check
description: "Issue triage and classification. Every issue surfaced during review, audit, or maintenance is classified as LEGITIMATE, NON-ISSUE, or UNCERTAIN before any action is taken."
author: "Parzival POV Module"

# Critical variables from config
config_source: "{project-root}/_ai-memory/pov/config.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
document_output_language: "{config_source}:document_output_language"
date: system-generated
oversight_path: "{config_source}:oversight_path"
output_folder: "{config_source}:pov_output_folder"

# Workflow components
installed_path: "{project-root}/_ai-memory/pov/workflows/cycles/legitimacy-check"
instructions: "{installed_path}/instructions.md"
validation: "{installed_path}/checklist.md"
template: false

variables:
  constraints_path: "{config_source}:constraints_path"
  workflows_path: "{config_source}:workflows_path"

tags: ["pov", "cycles", "legitimacy-check"]
standalone: true
