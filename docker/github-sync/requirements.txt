# GitHub Sync Service Dependencies
# Subset of main requirements.txt — only what the sync engine needs
# Last audited: 2026-04-01

# Vector Database
qdrant-client==1.17.1

# HTTP Client (for GitHubClient + EmbeddingClient)
httpx==0.28.1

# Environment Configuration
pydantic==2.12.5
pydantic-settings==2.13.1

# Monitoring
prometheus-client==0.24.1

# Retry logic
tenacity==9.1.4

# Langfuse observability (Wave 3, PLAN-011)
langfuse>=4.0.0,<4.1.0

# Required by memory module import chain (memory/__init__.py)
# BUG-086: memory/__init__.py unconditionally imports async_sdk_wrapper (anthropic)
# and logging_config (structlog). Any container importing memory.* needs these.
anthropic==0.86.0
structlog>=24.0.0,<26.0.0

# Token counting (memory.chunking.truncation import chain)
tiktoken>=0.5.0,<1.0.0

# SPEC-009: Security scanning dependencies
# Layer 2: detect-secrets for entropy-based secret detection
detect-secrets>=1.4.0

# Layer 3: SpaCy NER for person name detection
# Note: en_core_web_sm model downloaded in Dockerfile build step
spacy>=3.8.0,<4.0.0
